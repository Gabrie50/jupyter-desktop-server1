#!/bin/sh
cd "$HOME"

# Define a resolução do TurboVNC
export DISPLAY=:1
vncserver :1 -geometry 1920x1080 -depth 24 -localhost no

# Inicia o Fluxbox corretamente
exec /usr/bin/dbus-launch --exit-with-session fluxbox

